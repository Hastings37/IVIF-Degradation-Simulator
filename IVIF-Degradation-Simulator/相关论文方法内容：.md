

# Control Fusion
**数据集：**
使用的是包含有15种退化的DDL-12 
**验证集**：MSRS [30]、LLVIP [8]、RoadScene [35] 和 FMB [16]。  
对应的测试集规模为：MSRS 为 361 张，LLVIP 为 50 张，RoadScene 为 50 张，FMB 为 50 张。

**评价指标：**
EN、SD、VIF 和 Qabf
- **可见光图像（VI）退化：** 模糊、雨、过曝、低照度
    
- **红外图像（IR）退化：** 低对比度、随机噪声、条纹噪声

**复合退化场景**下的有效性，例如：

- VI：雨 + 雾
    
- VI：低照度 + 噪声
    
- VI：过曝
    
- IR：低对比度
    
- VI + IR：低照度（VI）+ 条纹噪声（IR）
    
- VI + IR：雨与雾（VI）+ 噪声（IR）

CLIP-IQA [34]、MUSIQ [10]、TReS [3]、EN 和 SD

使用图像恢复方法来解决不同的退化的问题： 
- OneRestore [6]：处理天气相关退化
    
- Instruct-IR [2]：处理光照相关退化
    
- DA-CLIP [18] 与 WDNN [4]：处理传感器相关退化





# Omni Fuse

### 数据集： 
基于 MFNet [28]、FMB [30] 和 LLVIP [29] 构建了所需的训练与测试数据集。具体而言，我们引入了**恢复池（restoration pool）**和**退化池（degradation pool）**的概念。
- **恢复池**由一系列图像恢复算法组成（如 QuadPrior [38] 和 Diff-Retinex [39]），用于从现有的退化多模态图像中构造干净版本；
    
- **退化池**包含各种退化函数（如高斯噪声、泊松噪声、条纹噪声等），用于从可用的干净多模态图像生成退化版本。

这里提到关于退化和恢复的方法主要针对的是低对比度的情况下的图像进行的； 

同时因为LLVIP中没有关于分割的标签内容，我们从MFNet和FMB中各自选择500对图像用在后面的可控的融合训练操作； 

模型的评估是在： 
- 跨模态退化场景
    
- 红外退化场景
    
- 可见光退化场景
    
- 无退化场景

170对图像上进行的评估操作； 

### 评价指标: 

SD [41]、PSNR、MI [42]、SCD [43] 和 SSIM [44]。

下游任务的性能的衡量主要是 
* 目标检测： mAP 
* 语义分割： mIoU

CLIP分数： 使用OpenAi 的视觉语言模型，同时理解文字和图像并且映射到同一个空间中，CLIP分数就是对应的图像的特征向量和文本特征向量之间的余弦相似度； （图像表达的内容和我们想要的是不是一致的形式的） ； 





# Text-DiFuse 

IVIF：使用 MSRS 数据集 [43]，包含 485 对训练图像和 100 对测试图像。
（实际上使用的是其中的一小部分的内容） 
通过随机翻转和随机裁剪等数据增强策略，训练样本被扩充至：

- IVIF：12,888 对
    
- MIF：6,408 对
此外，我们在 LLVIP 数据集 [15] 中的 60 对图像和 RoadScene 数据集 [49] 中的 25 对图像上评估模型的泛化性能。


**评价指标**：EN [39]、AG [3]、SD [38]、SCD [2] 和 VIF [11]。



# SIBA
**数据集**：我们使用 MSRS 数据集 [23] 的训练集。为了进一步增强训练数据，我们从 RoadScene 数据集 [33] 中随机选取 200 对图像作为补充。在评估阶段，我们使用 MSRS 测试集。此外，为了评估我们方法的泛化能力，我们在 **未进行任何重新训练或微调** 的情况下，对 M3FD [16] 和 TNO [25] 数据集进行测试。
由于 M3FD 数据集图像分辨率较高，一些对比方法在该数据集上运行时间极其漫长。为了加快测试过程，我们将图像缩放为原始分辨率的一半，并基于缩放后的图像分辨率报告所有方法在 M3FD 上的性能指标。




